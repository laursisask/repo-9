name: test-container-job
on: workflow_dispatch
jobs:
  container-test-job:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/bmacnaughton/centos7-devtoolset7:latest
      env:
        NODE_ENV: production
      # ports:
      #   - 80
      volumes:
        - ${{ github.workspace }}:/volume_mount
      # options: --cpus 1
    steps:
      - name: see what's around
        run: find /volume_mount -name '*' && find /home -type d
